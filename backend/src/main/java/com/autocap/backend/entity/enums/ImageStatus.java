package com.autocap.backend.entity.enums;

/**
 * Maps the check constraint on images.status:
 * 'uploaded' | 'processing' | 'completed' | 'failed'
 */
public enum ImageStatus {
    uploaded,
    processing,
    completed,
    failed
}
